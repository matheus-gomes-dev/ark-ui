import React from 'react';

import Avatar from './';

export default {
  title: 'Header/Avatar',
  component: Avatar
};

const Template = (args) => <Avatar {...args} />;

export const User = Template.bind({});